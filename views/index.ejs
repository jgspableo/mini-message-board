<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message Board</title>
  </head>
  <body>
    <% for (let i = 0; i < messages.length; i++) { %>
    <p>
      <%= messages[i].user + ": " + messages[i].text + " (" + messages[i].added
      + ")" %>
    </p>
    <% } %>
    <a href="/new">
      <button>Add message</button>
    </a>
  </body>
</html>
